# Зависимости
node_modules/

# Сборка
dist/
build/

# Секреты — НИКОГДА в репозиторий (anon-ключ публичен, но service_role и токены — нет)
.env
.env.*
!.env.example
supabase/.env
**/service_role*
*.key
telegram-token*

# Логи / временное
*.log
.DS_Store
.commitmsg.tmp

# IDE
.vscode/
.idea/
