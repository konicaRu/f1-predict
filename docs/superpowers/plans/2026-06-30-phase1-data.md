# Фаза 1 — Данные (импорт Jolpica). План реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Наполнить облачную БД `konicaRu_f1` реальными данными сезона 2026 из Jolpica — пилоты, календарь (22 гонки), результаты прошедших раундов как демо (`scored=false`).

**Architecture:** Node-скрипт `scripts/import/` (cloud-direct, как `scripts/db`): `fetch` Jolpica → UPSERT в облако через session pooler (pg). Идемпотентно. Чистая логика (дедлайн) — по TDD; интеграция — через `verify.js`.

**Tech Stack:** Node 22 (глобальный `fetch`), `pg`, Supabase Postgres (session pooler). Источник правды — `docs/superpowers/specs/2026-06-30-phase1-data-design.md`.

**Предусловие:** Фаза 0 в облаке (схема+RLS), `.env` с `SUPABASE_DB_URL` (session pooler) на месте.

---

## Карта файлов

| Файл | Ответственность |
|---|---|
| `supabase/migrations/0005_is_sprint.sql` | колонка `races.is_sprint` |
| `scripts/import/package.json` | зависимость `pg` |
| `scripts/import/teams.js` | карта `constructorId → team_color` (plan §16.9) |
| `scripts/import/deadlines.js` | `deadlineUtc(raceDate)` — чистая функция (TDD) |
| `scripts/import/lib.js` | pg-клиент (.env) с ретраем + `fetchJolpica(path)` |
| `scripts/import/import.js` | `node import.js [drivers\|calendar\|results\|all]` |
| `scripts/import/verify.js` | критерий готовности |
| `scripts/import/deadlines.test.js` | тесты дедлайна |

Все скрипты — CommonJS, читают `.env` из корня проекта (`path.join(__dirname,'..','..','.env')`).

---

## Task 1: Скаффолд `scripts/import` (package.json, teams, lib)

**Files:**
- Create: `scripts/import/package.json`, `scripts/import/teams.js`, `scripts/import/lib.js`

- [ ] **Step 1: package.json**

```json
{
  "name": "f1-predict-import",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "drivers": "node import.js drivers",
    "calendar": "node import.js calendar",
    "results": "node import.js results",
    "all": "node import.js all",
    "verify": "node verify.js",
    "test": "node deadlines.test.js"
  },
  "dependencies": { "pg": "^8.13.0" }
}
```

- [ ] **Step 2: teams.js (карта цветов, plan §16.9)**

```js
// constructorId (Jolpica) -> HEX цвета команды. Незнакомая команда -> серый фолбэк в lib.
module.exports = {
  mclaren: '#FF8000', ferrari: '#FF2D3C', mercedes: '#00D7B6', red_bull: '#4B7BEC',
  williams: '#5AA9FF', aston_martin: '#2BD589', alpine: '#2AA7E0', rb: '#8AA0FF',
  audi: '#19B3A6', haas: '#C5CAD2', cadillac: '#C7A35A',
};
```

- [ ] **Step 3: lib.js (pg с ретраем + fetchJolpica)**

```js
const fs = require('fs'); const path = require('path');
const { Client } = require('pg');

function connStr(){
  const env = fs.readFileSync(path.join(__dirname,'..','..','.env'),'utf8');
  const m = env.match(/^SUPABASE_DB_URL=(.+)$/m);
  if(!m) throw new Error('SUPABASE_DB_URL не найден в .env');
  return m[1].trim();
}
const sleep = ms => new Promise(r=>setTimeout(r,ms));
const transient = e => e.code===undefined || e.code==='57014' ||
  /terminated|ECONN|ETIMEDOUT|EPIPE|EAI_AGAIN|fetch failed|network/i.test(e.message||'');

// Jolpica REST с ретраем. path пример: '2026/drivers'
async function fetchJolpica(p){
  const url = `https://api.jolpi.ca/ergast/f1/${p}.json?limit=100`;
  for(let a=1;a<=5;a++){
    try{
      const res = await fetch(url, { headers:{'accept':'application/json'} });
      if(!res.ok) throw new Error('HTTP '+res.status);
      return await res.json();
    }catch(e){ if(a===5) throw e; await sleep(800*a); }
  }
}

// Персистентный pg-клиент с переподключением; q() ретраит транзиентные обрывы.
let client=null;
async function ensure(){
  if(client) return client;
  client = new Client({ connectionString:connStr(), ssl:{rejectUnauthorized:false}, connectionTimeoutMillis:20000, keepAlive:true });
  client.on('error',()=>{ client=null; });
  await client.connect();
  return client;
}
async function q(text, params){
  for(let a=1;a<=6;a++){
    try{ const c=await ensure(); return await c.query(text, params); }
    catch(e){ try{ if(client) await client.end(); }catch(_){}; client=null;
      if(!transient(e)||a===6) throw e; await sleep(1200*a); }
  }
}
async function close(){ if(client){ try{await client.end();}catch(_){}; client=null; } }

module.exports = { fetchJolpica, q, close };
```

- [ ] **Step 4: Smoke — установка и проверка fetch + pg**

Run:
```bash
cd scripts/import && npm install && node -e "const{fetchJolpica,q,close}=require('./lib');(async()=>{const d=await fetchJolpica('2026/drivers');console.log('drivers total',d.MRData.total);const r=await q('select count(*)::int c from drivers');console.log('db drivers',r.rows[0].c);await close();})()"
```
Expected: `drivers total 31` (или близко) и `db drivers 0` (таблица пустая). Ошибок нет.

- [ ] **Step 5: Commit**

```bash
git add scripts/import/package.json scripts/import/teams.js scripts/import/lib.js
git commit -m "feat(phase1): скаффолд scripts/import (lib: fetchJolpica + pg-ретрай, teams)"
```

---

## Task 2: Миграция `0005_is_sprint`

**Files:**
- Create: `supabase/migrations/0005_is_sprint.sql`

- [ ] **Step 1: Миграция**

```sql
-- 0005_is_sprint.sql — пометка спринт-уикендов (для тега SPRINT в UI).
alter table public.races add column if not exists is_sprint boolean not null default false;
```

- [ ] **Step 2: Применить к облаку (без wipe — idempotent alter)**

Run: `cd scripts/db && node runner.js applyfile "$(cd ../../ && pwd)/supabase/migrations/0005_is_sprint.sql"`
Expected: `applied: 0005_is_sprint.sql (1 stmts)`.

- [ ] **Step 3: Проверить колонку**

Run: `cd scripts/db && node runner.js sql "select column_name from information_schema.columns where table_name='races' and column_name='is_sprint'"`
Expected: одна строка `is_sprint`.

- [ ] **Step 4: Commit**

```bash
git add supabase/migrations/0005_is_sprint.sql
git commit -m "feat(phase1): миграция 0005 — races.is_sprint"
```

---

## Task 3: `deadlineUtc()` — чистая функция (TDD)

**Files:**
- Create: `scripts/import/deadlines.js`, `scripts/import/deadlines.test.js`

- [ ] **Step 1: Написать падающий тест**

```js
// deadlines.test.js — дедлайн = ближайший четверг ПЕРЕД гонкой, 20:00:00 UTC.
const assert = require('assert');
const { deadlineUtc } = require('./deadlines');

let fail = 0;
function check(raceDate, expected){
  const got = deadlineUtc(raceDate);
  const ok = got === expected;
  console.log(`${ok?'PASS':'FAIL'}  ${raceDate} -> ${got}` + (ok?'':`  (ждали ${expected})`));
  if(!ok) fail++;
}
// Британия (вс 2026-07-05) -> чт 2026-07-02 20:00Z
check('2026-07-05', '2026-07-02T20:00:00.000Z');
// Австралия (вс 2026-03-08) -> чт 2026-03-05
check('2026-03-08', '2026-03-05T20:00:00.000Z');
// гонка в субботу (2026-07-04, dow6) -> чт 2026-07-02
check('2026-07-04', '2026-07-02T20:00:00.000Z');
console.log(fail===0?'ВСЕ PASS':`ПРОВАЛЕНО: ${fail}`);
process.exit(fail===0?0:1);
```

- [ ] **Step 2: Запустить — убедиться, что падает**

Run: `cd scripts/import && node deadlines.test.js`
Expected: FAIL — `Cannot find module './deadlines'` или `deadlineUtc is not a function`.

- [ ] **Step 3: Реализация**

```js
// deadlines.js — deadlineUtc('YYYY-MM-DD') -> ISO ближайшего четверга перед датой, 20:00:00Z.
function deadlineUtc(raceDate){
  const d = new Date(raceDate + 'T00:00:00Z');
  const t = new Date(d);
  do { t.setUTCDate(t.getUTCDate() - 1); } while (t.getUTCDay() !== 4); // 4 = четверг
  t.setUTCHours(20,0,0,0);
  return t.toISOString();
}
module.exports = { deadlineUtc };
```

- [ ] **Step 4: Запустить — PASS**

Run: `cd scripts/import && node deadlines.test.js`
Expected: `ВСЕ PASS` (3/3).

- [ ] **Step 5: Commit**

```bash
git add scripts/import/deadlines.js scripts/import/deadlines.test.js
git commit -m "feat(phase1): deadlineUtc (четверг 20:00 UTC) + тесты"
```

---

## Task 4: Импорт пилотов (`import.js drivers`)

**Files:**
- Create: `scripts/import/import.js`

- [ ] **Step 1: import.js с командой drivers**

Команда `team` пилота берётся из `/2026/driverStandings` (Driver + Constructors[]).

```js
const { fetchJolpica, q, close } = require('./lib');
const { deadlineUtc } = require('./deadlines');
const TEAMS = require('./teams');

// driverId -> {constructorId, name} из driverStandings (кто уже ехал)
async function driverTeams(){
  const d = await fetchJolpica('2026/driverStandings');
  const lists = d.MRData.StandingsTable.StandingsLists;
  const map = {};
  if(lists && lists[0]){
    for(const s of lists[0].DriverStandings){
      const c = s.Constructors[s.Constructors.length-1];
      map[s.Driver.driverId] = { constructorId:c.constructorId, name:c.name };
    }
  }
  return map;
}

async function importDrivers(){
  const d = await fetchJolpica('2026/drivers');
  const teams = await driverTeams();
  let n=0, gray=0;
  for(const dr of d.MRData.DriverTable.Drivers){
    const t = teams[dr.driverId];
    const color = t ? (TEAMS[t.constructorId] || '#888') : null;
    if(t && !TEAMS[t.constructorId]){ gray++; console.warn('нет цвета для команды', t.constructorId); }
    await q(
      `insert into drivers(id,code,name,team,team_color,active) values($1,$2,$3,$4,$5,true)
       on conflict (id) do update set code=excluded.code, name=excluded.name,
         team=excluded.team, team_color=excluded.team_color, active=excluded.active`,
      [dr.driverId, dr.code, `${dr.givenName} ${dr.familyName}`, t?t.name:null, color]
    );
    n++;
  }
  console.log(`drivers: ${n} upsert (${gray} без цвета)`);
}

async function main(){
  const cmd = process.argv[2];
  if(cmd==='drivers') await importDrivers();
  else { console.error('usage: drivers|calendar|results|all'); process.exit(2); }
  await close();
}
main().catch(async e=>{ console.error('ERR', e.code||'', e.message); await close(); process.exit(1); });
```

- [ ] **Step 2: Запустить импорт пилотов**

Run: `cd scripts/import && node import.js drivers`
Expected: `drivers: 31 upsert (0 без цвета)` (число пилотов ~31, без серых).

- [ ] **Step 3: Проверить в БД**

Run: `cd scripts/db && node runner.js sql "select count(*)::int total, count(team_color)::int colored from drivers"`
Expected: `total` ~31, `colored` ≥ 22 (боевой состав с цветами). Спот-чек:
Run: `cd scripts/db && node runner.js sql "select code,team,team_color from drivers where id='russell'"`
Expected: `RUS | Mercedes | #00D7B6`.

- [ ] **Step 4: Commit**

```bash
git add scripts/import/import.js
git commit -m "feat(phase1): импорт пилотов из Jolpica (drivers + команды/цвета)"
```

---

## Task 5: Импорт календаря (`import.js calendar`)

**Files:**
- Modify: `scripts/import/import.js`

- [ ] **Step 1: Добавить importCalendar и ветку команды**

Вставить функцию перед `main`:

```js
async function importCalendar(){
  const d = await fetchJolpica('2026/races');
  let n=0, sprint=0;
  for(const r of d.MRData.RaceTable.Races){
    const dt = `${r.date}T${r.time||'12:00:00Z'}`;       // Jolpica отдаёт UTC
    const isSprint = !!r.Sprint;
    if(isSprint) sprint++;
    await q(
      `insert into races(season,round,name,race_datetime_utc,deadline_utc,is_sprint,status,scored)
         values(2026,$1,$2,$3,$4,$5,'demo',false)
       on conflict (season,round) do update set name=excluded.name,
         race_datetime_utc=excluded.race_datetime_utc, deadline_utc=excluded.deadline_utc,
         is_sprint=excluded.is_sprint`,
      [Number(r.round), r.raceName, dt, deadlineUtc(r.date), isSprint]
    );
    n++;
  }
  console.log(`races: ${n} upsert (${sprint} спринтов)`);
}
```

И в `main` добавить ветку:
```js
  else if(cmd==='calendar') await importCalendar();
```
(строку `else if` поставить ПЕРЕД финальным `else { console.error(...) }`)

- [ ] **Step 2: Запустить импорт календаря**

Run: `cd scripts/import && node import.js calendar`
Expected: `races: 22 upsert (N спринтов)` где N≥1.

- [ ] **Step 3: Проверить календарь и дедлайн Британии**

Run: `cd scripts/db && node runner.js sql "select count(*)::int races from races"`
Expected: `races` = 22.
Run: `cd scripts/db && node runner.js sql "select round,name,deadline_utc,is_sprint from races where round=9"`
Expected: round 9, British Grand Prix, `deadline_utc` = `2026-07-02T20:00:00+00` (20:00 UTC), `is_sprint` соответствует.

- [ ] **Step 4: Commit**

```bash
git add scripts/import/import.js
git commit -m "feat(phase1): импорт календаря 2026 (deadline, is_sprint)"
```

---

## Task 6: Импорт результатов + пометка демо (`import.js results`)

**Files:**
- Modify: `scripts/import/import.js`

- [ ] **Step 1: Добавить importResults и ветку**

```js
async function importResults(){
  let loaded=0;
  for(let round=1; round<=22; round++){
    const d = await fetchJolpica(`2026/${round}/results`);
    const races = d.MRData.RaceTable.Races;
    if(!races.length || !races[0].Results || races[0].Results.length < 10) continue; // не завершён
    const top10 = races[0].Results
      .slice().sort((a,b)=>Number(a.position)-Number(b.position))
      .slice(0,10).map(x=>x.Driver.driverId);
    await q(
      `insert into results(race_id, positions, status, fetched_at)
         select id, $2::jsonb, 'final', now() from races where season=2026 and round=$1
       on conflict (race_id) do update set positions=excluded.positions, status='final', fetched_at=now()`,
      [round, JSON.stringify(top10)]
    );
    await q(`update races set status='resulted' where season=2026 and round=$1`, [round]);
    loaded++;
  }
  console.log(`results: ${loaded} завершённых раундов загружено`);
}
```
И ветка в `main`:
```js
  else if(cmd==='results') await importResults();
```

- [ ] **Step 2: Запустить импорт результатов**

Run: `cd scripts/import && node import.js results`
Expected: `results: 8 завершённых раундов загружено` (на 2026-06-30).

- [ ] **Step 3: Проверить результаты**

Run: `cd scripts/db && node runner.js sql "select count(*)::int rows from results"`
Expected: `rows` = 8.
Run: `cd scripts/db && node runner.js sql "select jsonb_array_length(positions) len, positions->>0 winner from results r join races ra on ra.id=r.race_id where ra.round=1"`
Expected: `len` = 10, `winner` = `russell`.

- [ ] **Step 4: Commit**

```bash
git add scripts/import/import.js
git commit -m "feat(phase1): импорт результатов прошедших раундов (демо, scored=false)"
```

---

## Task 7: `import.js all` + `verify.js` (критерий готовности)

**Files:**
- Modify: `scripts/import/import.js`
- Create: `scripts/import/verify.js`

- [ ] **Step 1: Ветка `all` в import.js**

В `main` добавить перед финальным `else`:
```js
  else if(cmd==='all'){ await importDrivers(); await importCalendar(); await importResults(); }
```

- [ ] **Step 2: verify.js**

```js
const { q, close } = require('./lib');
let fail=0;
function check(name, ok, info){ console.log(`${ok?'PASS':'FAIL'}  ${name}  — ${info}`); if(!ok) fail++; }
(async()=>{
  const races = (await q('select count(*)::int c from races')).rows[0].c;
  check('races=22', races===22, `races=${races}`);

  const dl = (await q("select to_char(deadline_utc at time zone 'UTC','YYYY-MM-DD\"T\"HH24:MI:SS') d from races where round=9")).rows[0];
  check('Британия дедлайн', dl && dl.d==='2026-07-02T20:00:00', `${dl&&dl.d}`);

  const res = (await q('select count(*)::int c from results')).rows[0].c;
  check('results=8', res===8, `results=${res}`);

  const lens = (await q('select count(*)::int c from results where jsonb_array_length(positions)<>10')).rows[0].c;
  check('все результаты по 10', lens===0, `кривых=${lens}`);

  const w = (await q("select positions->>0 w from results r join races ra on ra.id=r.race_id where ra.round=1")).rows[0];
  check('round1 winner=russell', w && w.w==='russell', `${w&&w.w}`);

  const noColor = (await q(`select count(*)::int c from drivers where id in
    (select distinct jsonb_array_elements_text(positions) from results) and (team_color is null or team_color='#888')`)).rows[0].c;
  check('боевые пилоты с цветом', noColor===0, `без цвета=${noColor}`);

  const sprint = (await q('select count(*)::int c from races where is_sprint')).rows[0].c;
  check('спринты помечены', sprint>=1, `спринтов=${sprint}`);

  await close();
  console.log(`\n=== ИТОГ: ${fail===0?'ВСЁ PASS':fail+' FAIL'} ===`);
  process.exit(fail===0?0:1);
})().catch(async e=>{ console.error('ERR',e.message); await close(); process.exit(1); });
```

- [ ] **Step 3: Полный прогон + проверка идемпотентности**

Run: `cd scripts/import && node import.js all && node verify.js`
Expected: импорт отрабатывает, `verify` → все PASS.

Run второй раз (идемпотентность): `cd scripts/import && node import.js all && node verify.js`
Expected: те же числа (races=22, results=8), все PASS — дублей нет.

- [ ] **Step 4: Commit**

```bash
git add scripts/import/import.js scripts/import/verify.js
git commit -m "feat(phase1): import.js all + verify.js (критерий готовности Фазы 1)"
```

---

## Task 8: README + git save

**Files:**
- Create: `scripts/import/README.md`
- Modify: `MEMORY.md`, `ARCHITECTURE.md`

- [ ] **Step 1: scripts/import/README.md**

```markdown
# scripts/import — загрузка данных сезона из Jolpica (Фаза 1)

Cloud-direct: тянет Jolpica и UPSERT'ит в облако (session pooler, см. `../db/README.md` про `.env`).

## Команды (`cd scripts/import && npm install`)
- `npm run drivers`  — пилоты + команды/цвета.
- `npm run calendar` — 22 гонки (deadline = чт 20:00 UTC, is_sprint).
- `npm run results`  — результаты завершённых раундов (демо, scored=false).
- `npm run all`      — всё подряд. Идемпотентно (UPSERT).
- `npm run verify`   — критерий готовности Фазы 1.
- `npm test`         — юнит-тест deadlineUtc.

Источник истины по данным — Jolpica (не хардкод грида из plan §16.10).
```

- [ ] **Step 2: Обновить MEMORY.md (статус + лог) и ARCHITECTURE.md (changelog)**

В `MEMORY.md` → «Статус»: отметить, что Фаза 1 (данные) выполнена — пилоты/календарь/результаты в облаке, `verify` зелёный. В «Лог сессий» добавить запись 2026-06-30 про импорт. В `ARCHITECTURE.md` → Changelog: строка про Фазу 1 и `scripts/import/`.

- [ ] **Step 3: Commit (git save)**

```bash
git add scripts/import/README.md MEMORY.md ARCHITECTURE.md
git commit -m "docs(phase1): README импорта + git save (Фаза 1 — данные загружены)"
```

---

## Самопроверка плана (выполнена)

- **Покрытие спеки:** §1 архитектура→T1; §2 is_sprint→T2; §3 пилоты→T4; §4 календарь→T5; §5 результаты→T6; §6 verify→T7. Решения P1–P8 отражены (P3 дедлайн→T3/T5, P5 цвета→T1/T4, P6 sprint→T2/T5, P7 пул не трогаем — нет задачи на пул, верно, P8 команда из standings→T4).
- **Плейсхолдеров нет:** весь код конкретный.
- **Согласованность:** `fetchJolpica`/`q`/`close` из lib.js используются одинаково; `deadlineUtc` определён в T3 и вызывается в T5; имена колонок совпадают со схемой Фазы 0.

**Риск исполнителю:** формат `driverStandings` (массив `Constructors`) — если у пилота несколько команд за сезон, берём последнюю. Число завершённых раундов (8) зависит от даты прогона; verify ждёт 8 на 2026-06-30 — если прогон позже и раундов больше, поправить ожидание в verify.
