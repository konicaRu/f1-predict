# ИИ-игрок в лиге (GridBot) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** GridBot — обычный участник лиги (ходит в `predictions`/`scores` как любой человек), чей
еженедельный прогноз собирает cloud-direct Node-скрипт: данные из своей БД + прошлые сезоны F1 через
Jolpica → промпт в Gemini API (бесплатный тариф, JSON-режим) → валидация → сохранение; при сбое —
эвристика-фолбэк вместо падения; по четвергам — короткое объяснение в общий Telegram-чат.

**Architecture:** Новая папка `scripts/ai-player/` (тот же самостоятельный cloud-direct паттерн, что
`scripts/telegram`/`scripts/autoresults`/`scripts/import` — свой `package.json`, читает `SUPABASE_DB_URL`
из корневого `.env`, пишет в БД в обход RLS через прямое подключение). Аккаунт GridBot заводится
миграцией напрямую в `auth.users`+`public.users` (без штатного Supabase Auth signup — обоснование и
эмпирическая проверка в Task 1). Запуск — новый режим `aiplayer` в уже существующем workflow
`.github/workflows/telegram-notify.yml`, на существующем четверговом утреннем cron-триггере, перед
`deadline`.

**Tech Stack:** Node.js (CommonJS), `pg` (прямое подключение к Postgres), нативный `fetch` (Node 20+,
без SDK — ни для Jolpica, ни для Gemini, ни для Telegram, по образцу `scripts/telegram/lib.js`).

Сверено с `docs/constitution.md` (§4 секреты не в git, §6 YAGNI). Спека:
`docs/superpowers/specs/2026-07-24-ai-player-design.md`.

---

### Task 1: Аккаунт GridBot (миграция + тест)

**Files:**
- Create: `supabase/migrations/0014_gridbot_user.sql`
- Create: `scripts/db/gridbot.test.js`
- Modify: `scripts/db/package.json`

**Контекст:** прямая вставка в `auth.users` в обход штатного Supabase Auth signup — прецедента в
проекте нет. Уже проверено эмпирически в этой сессии (rolled-back транзакция через
`scripts/db/runner.js sql`): единственная `NOT NULL` колонка без дефолта в `auth.users` — `id`;
собственных триггеров на `auth.users` в этом проекте нет. Ниже — точный проверенный INSERT.

- [ ] **Step 1: Написать миграцию**

Создать `supabase/migrations/0014_gridbot_user.sql`:

```sql
-- 0014_gridbot_user.sql — аккаунт GridBot (ИИ-игрок лиги), играет как обычный участник.
-- Прямая вставка в auth.users в обход штатного Supabase Auth signup — обоснованно и проверено
-- эмпирически (rolled-back транзакция, сессия 2026-07-24): единственная NOT NULL без дефолта
-- колонка — id; собственных триггеров на auth.users в этом проекте нет.
insert into auth.users (
  id, instance_id, aud, role, email, encrypted_password, email_confirmed_at,
  raw_app_meta_data, raw_user_meta_data, created_at, updated_at
) values (
  '8093e42f-cc5e-4c18-8aa8-2dfa50e972c1',
  '00000000-0000-0000-0000-000000000000',
  'authenticated', 'authenticated', 'gridbot@f1-predict.local', '',
  now(), '{"provider":"none","providers":[]}'::jsonb, '{}'::jsonb, now(), now()
)
on conflict (id) do nothing;

insert into public.users (id, display_name, is_admin) values
  ('8093e42f-cc5e-4c18-8aa8-2dfa50e972c1', 'GridBot', false)
on conflict (id) do nothing;
```

- [ ] **Step 2: Применить миграцию к облачной БД**

Run: `cd scripts/db && node runner.js applyfile ../../supabase/migrations/0014_gridbot_user.sql`
Expected: `applied: 0014_gridbot_user.sql (2 stmts)`.

**Не запускать `npm run rebuild`** — дропает и пересоздаёт ВСЕ таблицы с реальными данными сезона.
Только `applyfile`.

- [ ] **Step 3: Написать тест — GridBot существует и его прогноз проходит серверную валидацию**

Создать `scripts/db/gridbot.test.js` (по образцу `scripts/db/predicted_user_ids.test.js` — вся
проверка в одной транзакции с `rollback`, синтетические гонка/пилоты с высоким id):

```js
// Тест миграции 0014: аккаунт GridBot существует, его прогноз проходит те же серверные проверки
// (validate_prediction), что и прогноз обычного игрока — без реального обращения к Gemini/Jolpica.
const fs = require('fs'); const path = require('path');
const { Client } = require('pg');
const env = fs.readFileSync(path.join(__dirname,'..','..','.env'),'utf8');
const connStr = env.match(/^SUPABASE_DB_URL=(.+)$/m)[1].trim();

const GRIDBOT_ID = '8093e42f-cc5e-4c18-8aa8-2dfa50e972c1';
const R = 900000301; // высокий id, чтобы не конфликтовать с реальными гонками

const SQL = `
begin;
set local statement_timeout='25s';
do $$
declare
  n int; names text[]:='{}'; passed boolean[]:='{}'; infos text[]:='{}';
begin
  create temp table _t4(name text, passed boolean, info text) on commit drop;

  select count(*) into n from public.users where id='${GRIDBOT_ID}' and display_name='GridBot' and is_admin=false;
  names:=array_append(names,'GridBot существует в users'); passed:=array_append(passed,(n=1)); infos:=array_append(infos,'найдено='||n);

  insert into drivers(id,code,name) select 'gb'||g,'GB'||g,'Drv'||g from generate_series(1,10) g
    on conflict (id) do nothing;
  insert into races(id,round,name,deadline_utc,status) overriding system value values
    (${R},9301,'GridBotTest',now()+interval '2 days','open');
  insert into race_driver_pool(race_id,driver_id) select ${R},'gb'||g from generate_series(1,10) g;

  begin
    insert into predictions(user_id,race_id,positions) values
      ('${GRIDBOT_ID}',${R}, (select jsonb_agg('gb'||g order by g) from generate_series(1,10) g));
    names:=array_append(names,'прогноз GridBot проходит validate_prediction'); passed:=array_append(passed,true); infos:=array_append(infos,'ok');
  exception when others then
    names:=array_append(names,'прогноз GridBot проходит validate_prediction'); passed:=array_append(passed,false); infos:=array_append(infos,sqlerrm);
  end;

  insert into _t4 select * from unnest(names,passed,infos);
end $$;
select name, passed, info from _t4 order by name;
rollback;
`;
function sleep(ms){return new Promise(r=>setTimeout(r,ms));}
function pickRows(res){ const arr=Array.isArray(res)?res:[res]; const r=arr.reverse().find(x=>x.rows&&x.rows.length); return r?r.rows:[]; }
async function killOrphans(){
  const c=new Client({connectionString:connStr,ssl:{rejectUnauthorized:false},connectionTimeoutMillis:15000}); c.on('error',()=>{});
  try{await c.connect(); await c.query("select pg_terminate_backend(pid) from pg_stat_activity where datname=current_database() and state like '%idle in transaction%' and pid<>pg_backend_pid()");}catch(_){}
  finally{try{await c.end();}catch(_){}}
}
async function once(){
  const c=new Client({connectionString:connStr,ssl:{rejectUnauthorized:false},connectionTimeoutMillis:20000,keepAlive:true}); c.on('error',()=>{});
  await c.connect();
  try{ return pickRows(await c.query(SQL)); } finally{ try{await c.end();}catch(_){} }
}
(async()=>{
  let rows;
  for(let a=1;a<=6;a++){
    try{ rows=await once(); break; }
    catch(e){ console.error(`attempt ${a}/6: ${e.code||''} ${e.message}`); if(a===6){console.error('сдаюсь');process.exit(1);} await killOrphans(); await sleep(2000*a); }
  }
  let pass=0,fail=0;
  for(const r of rows){ const ok=r.passed===true; ok?pass++:fail++; console.log(`${ok?'PASS':'FAIL'}  ${r.name}  — ${r.info}`); }
  console.log(`\n=== ИТОГ: ${pass} PASS, ${fail} FAIL (строк ${rows.length}/2) ===`);
  process.exit(fail===0&&rows.length===2?0:1);
})();
```

- [ ] **Step 4: Добавить npm-скрипт**

В `scripts/db/package.json` в объект `"scripts"` добавить (по образцу соседних, не входящих в
агрегатный `test`):

```json
    "test:gridbot": "node gridbot.test.js",
```

- [ ] **Step 5: Прогнать тест**

Run: `cd scripts/db && npm run test:gridbot`
Expected: `=== ИТОГ: 2 PASS, 0 FAIL (строк 2/2) ===`, exit code 0.

- [ ] **Step 6: Commit**

```bash
git add supabase/migrations/0014_gridbot_user.sql scripts/db/gridbot.test.js scripts/db/package.json
git commit -m "feat(db): аккаунт GridBot — ИИ-игрок лиги, обычный участник"
```

---

### Task 2: Каркас `scripts/ai-player/` (подключения: БД, Jolpica, Gemini, Telegram)

**Files:**
- Create: `scripts/ai-player/package.json`
- Create: `scripts/ai-player/lib.js`

- [ ] **Step 1: Создать `scripts/ai-player/package.json`**

```json
{
  "name": "f1-predict-ai-player",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "predict": "node predict.js",
    "test": "node predict.test.js"
  }
}
```

- [ ] **Step 2: Установить зависимости**

Run: `cd scripts/ai-player && npm install pg`
Expected: создаются `node_modules/`, `package-lock.json`; `package.json` дополняется
`dependencies.pg` с резолвнутой версией.

- [ ] **Step 3: Написать `scripts/ai-player/lib.js`**

```js
const fs = require('fs'); const path = require('path');
const { Client } = require('pg');

function readEnv(key) {
  const env = fs.readFileSync(path.join(__dirname, '..', '..', '.env'), 'utf8');
  const m = env.match(new RegExp(`^${key}=(.+)$`, 'm'));
  if (!m) throw new Error(`${key} не найден в .env`);
  return m[1].trim();
}

const sleep = ms => new Promise(r => setTimeout(r, ms));
const transient = e => e.code === undefined || e.code === '57014' ||
  /terminated|ECONN|ETIMEDOUT|EPIPE|EAI_AGAIN|fetch failed|network/i.test(e.message || '');

let client = null;
async function ensure() {
  if (client) return client;
  client = new Client({ connectionString: readEnv('SUPABASE_DB_URL'), ssl: { rejectUnauthorized: false }, connectionTimeoutMillis: 20000, keepAlive: true });
  client.on('error', () => { client = null; });
  await client.connect();
  return client;
}
async function q(text, params) {
  for (let a = 1; a <= 6; a++) {
    try { const c = await ensure(); return await c.query(text, params); }
    catch (e) {
      try { if (client) await client.end(); } catch (_) { /* уже закрыт */ }
      client = null;
      if (!transient(e) || a === 6) throw e;
      await sleep(1200 * a);
    }
  }
}
async function close() { if (client) { try { await client.end(); } catch (_) { /* уже закрыт */ } client = null; } }

// Jolpica REST с ретраем. p пример: '2025/driverStandings'
async function fetchJolpica(p) {
  const url = `https://api.jolpi.ca/ergast/f1/${p}.json?limit=100`;
  for (let a = 1; a <= 5; a++) {
    try {
      const res = await fetch(url, { headers: { accept: 'application/json' } });
      if (!res.ok) throw new Error('HTTP ' + res.status);
      return await res.json();
    } catch (e) { if (a === 5) throw e; await sleep(800 * a); }
  }
}

// Gemini API, структурированный JSON-режим — модель обязана вернуть top10 (10 кодов) + reasoning.
async function askGemini(prompt) {
  const apiKey = readEnv('GEMINI_API_KEY');
  const model = process.env.GEMINI_MODEL || 'gemini-2.0-flash';
  const url = `https://generativelanguage.googleapis.com/v1beta/models/${model}:generateContent?key=${apiKey}`;
  const body = {
    contents: [{ parts: [{ text: prompt }] }],
    generationConfig: {
      responseMimeType: 'application/json',
      responseSchema: {
        type: 'OBJECT',
        properties: {
          top10: { type: 'ARRAY', items: { type: 'STRING' }, minItems: 10, maxItems: 10 },
          reasoning: { type: 'STRING' },
        },
        required: ['top10', 'reasoning'],
      },
    },
  };
  const res = await fetch(url, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(body) });
  if (!res.ok) throw new Error(`Gemini HTTP ${res.status}: ${await res.text()}`);
  const data = await res.json();
  const text = data && data.candidates && data.candidates[0] && data.candidates[0].content
    && data.candidates[0].content.parts && data.candidates[0].content.parts[0]
    && data.candidates[0].content.parts[0].text;
  if (!text) throw new Error('Gemini: пустой ответ');
  return JSON.parse(text);
}

function escapeHtml(s) {
  return String(s).replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;');
}

async function sendTelegram(text) {
  const token = readEnv('TELEGRAM_BOT_TOKEN');
  const chatId = readEnv('TELEGRAM_CHAT_ID');
  const res = await fetch(`https://api.telegram.org/bot${token}/sendMessage`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ chat_id: chatId, text, parse_mode: 'HTML' }),
  });
  const data = await res.json();
  if (!data.ok) throw new Error(`Telegram API error: ${JSON.stringify(data)}`);
  return data;
}

module.exports = { readEnv, q, close, fetchJolpica, askGemini, escapeHtml, sendTelegram };
```

`sendTelegram`/`escapeHtml` продублированы из `scripts/telegram/lib.js`, а не заимпортированы оттуда
напрямую — каждая папка `scripts/*` в этом проекте самостоятельна (свой `package.json`/`node_modules`,
см. `README.md`: «В каждой папке нужен свой `npm install`»), кросс-папочный `require` создал бы
неявную связь между независимыми под-проектами. Тот же приём уже применён трижды в проекте
(`scripts/telegram/lib.js`, `scripts/autoresults/lib.js`, `scripts/import/lib.js` — у каждого свои
почти одинаковые `q()`/`readEnv`).

- [ ] **Step 4: Синтаксическая проверка**

Run: `cd scripts/ai-player && node --check lib.js`
Expected: без вывода, код завершения 0.

- [ ] **Step 5: Commit**

```bash
git add scripts/ai-player/package.json scripts/ai-player/package-lock.json scripts/ai-player/lib.js
git commit -m "feat(ai-player): каркас — подключение к БД, Jolpica, Gemini, Telegram"
```

---

### Task 3: `predict.js` — сбор данных, промпт, валидация, фолбэк, сохранение

**Files:**
- Create: `scripts/ai-player/predict.js`
- Create: `scripts/ai-player/predict.test.js`
- Modify: `scripts/ai-player/package.json`

- [ ] **Step 1: Написать тест на чистую логику (валидация топ-10 + фолбэк-сортировка)**

Создать `scripts/ai-player/predict.test.js`:

```js
// Тест чистой логики predict.js (без сети/БД): валидация ответа модели и фолбэк-эвристика.
const { isValidTop10, fallbackTop10, escapeHtml } = require('./predict');

const pool = [
  { id: 'd1', code: 'VER', standing: 1 },
  { id: 'd2', code: 'NOR', standing: 2 },
  { id: 'd3', code: 'LEC', standing: 3 },
  { id: 'd4', code: 'HAM', standing: 4 },
  { id: 'd5', code: 'PIA', standing: 5 },
  { id: 'd6', code: 'RUS', standing: 6 },
  { id: 'd7', code: 'SAI', standing: 7 },
  { id: 'd8', code: 'ALO', standing: null },
  { id: 'd9', code: 'GAS', standing: null },
  { id: 'd10', code: 'STR', standing: 8 },
  { id: 'd11', code: 'ANT', standing: 9 },
];
const validTop10 = ['VER', 'NOR', 'LEC', 'HAM', 'PIA', 'RUS', 'SAI', 'ALO', 'GAS', 'STR'];

const cases = [
  ['валидный топ-10 из пула', isValidTop10(validTop10, pool), true],
  ['не 10 элементов', isValidTop10(validTop10.slice(0, 9), pool), false],
  ['дубль вместо одного из кодов', isValidTop10([...validTop10.slice(0, 9), 'VER'], pool), false],
  ['код не из пула', isValidTop10([...validTop10.slice(0, 9), 'XXX'], pool), false],
  ['не массив', isValidTop10('VER,NOR', pool), false],
];

let pass = 0, fail = 0;
for (const [name, actual, expected] of cases) {
  const ok = actual === expected;
  ok ? pass++ : fail++;
  console.log(`${ok ? 'PASS' : 'FAIL'}  ${name}  — ожидали ${expected}, получили ${actual}`);
}

const fb = fallbackTop10(pool);
{
  // 9 гонщиков с реальным standing (VER..ANT) + ALO (standing=null, но первый по алфавиту среди
  // null) = 10; GAS (тоже null, но проигрывает ALO по алфавиту) — 11-й, за бортом топ-10.
  const ok = fb.length === 10 && fb[0] === 'VER' && fb[fb.length - 1] === 'ALO'
    && new Set(fb).size === 10 && !fb.includes('GAS');
  ok ? pass++ : fail++;
  console.log(`${ok ? 'PASS' : 'FAIL'}  fallbackTop10: топ по standing, без места — в конец  — ${fb.join('-')}`);
}

{
  const ok = escapeHtml('<b>x</b> & "y"') === '&lt;b&gt;x&lt;/b&gt; &amp; "y"';
  ok ? pass++ : fail++;
  console.log(`${ok ? 'PASS' : 'FAIL'}  escapeHtml экранирует < > &`);
}

console.log(`\nВСЕГО: ${pass} PASS, ${fail} FAIL`);
process.exit(fail === 0 ? 0 : 1);
```

- [ ] **Step 2: Запустить тест — убедиться, что падает**

Run: `cd scripts/ai-player && node predict.test.js`
Expected: `Error: Cannot find module './predict'` (файла ещё нет).

- [ ] **Step 3: Написать `scripts/ai-player/predict.js`**

```js
const { q, close, fetchJolpica, askGemini, escapeHtml, sendTelegram } = require('./lib');

const PAST_SEASONS = [2023, 2024, 2025];

async function openRaceThisWeek() {
  const { rows } = await q(`
    select id, round, name, season, deadline_utc
    from races
    where status = 'open'
      and date_trunc('week', deadline_utc at time zone 'Europe/Moscow')
        = date_trunc('week', now() at time zone 'Europe/Moscow')
    order by round
    limit 1
  `);
  return rows[0] || null;
}

async function poolDrivers(raceId) {
  const { rows } = await q(`
    select d.id, d.code, d.name, d.team, d.standing
    from race_driver_pool p
    join drivers d on d.id = p.driver_id
    where p.race_id = $1
    order by coalesce(d.standing, 999), d.code
  `, [raceId]);
  return rows;
}

async function seasonResults(season) {
  const { rows } = await q(`
    select r.round, r.name, res.positions
    from results res
    join races r on r.id = res.race_id
    where r.season = $1 and res.positions is not null
    order by r.round
  `, [season]);
  return rows;
}

// Map(driverId -> Map(season -> {position, points})). Сезон, где гонщика нет в списке -> не участвовал.
async function pastSeasonStandings(driverIds) {
  const out = new Map(driverIds.map((id) => [id, new Map()]));
  for (const season of PAST_SEASONS) {
    let data;
    try {
      data = await fetchJolpica(`${season}/driverStandings`);
    } catch (e) {
      console.warn(`aiplayer: Jolpica ${season} standings недоступны: ${e.message}`);
      continue;
    }
    const list = (data && data.MRData && data.MRData.StandingsTable
      && data.MRData.StandingsTable.StandingsLists && data.MRData.StandingsTable.StandingsLists[0]
      && data.MRData.StandingsTable.StandingsLists[0].DriverStandings) || [];
    for (const entry of list) {
      const id = entry.Driver.driverId;
      if (out.has(id)) {
        out.get(id).set(season, { position: Number(entry.position), points: Number(entry.points) });
      }
    }
  }
  return out;
}

function buildPrompt(race, pool, driverCodeOf, ownResults, standingsMap) {
  const poolLines = pool.map((d) => {
    const seasons = standingsMap.get(d.id);
    const isRookie = seasons.size === 0;
    if (isRookie) {
      return `- ${d.code} (${d.name}, ${d.team || 'без команды'}) — НОВИЧОК в F1, данных о прошлых `
        + `сезонах F1 нет; используй свои общие знания о его карьере в младших сериях (напр. Ф2/Ф3), `
        + `если они у тебя есть`;
    }
    const history = PAST_SEASONS.map((s) => {
      const st = seasons.get(s);
      return st ? `${s}: P${st.position} (${st.points} очк.)` : `${s}: не участвовал`;
    }).join(', ');
    return `- ${d.code} (${d.name}, ${d.team || 'без команды'}): ${history}`;
  }).join('\n');

  const ownLines = ownResults.length === 0
    ? 'ещё не было завершённых гонок в этом сезоне'
    : ownResults.map((r) => `Раунд ${r.round} (${r.name}): топ-10 = `
        + r.positions.map((id) => driverCodeOf.get(id) || id).join('-')).join('\n');

  return `Ты — участник фан-лиги прогнозов Формулы-1. Составь прогноз топ-10 для гонки `
    + `"${race.name}" (раунд ${race.round}, сезон ${race.season}).\n\n`
    + `Состав пилотов этой гонки (ровно из этого списка выбери топ-10, никого другого):\n${poolLines}\n\n`
    + `Результаты уже прошедших гонок этого сезона:\n${ownLines}\n\n`
    + `Верни ровно 10 РАЗНЫХ кодов пилотов из списка выше, в порядке от P1 до P10, плюс короткое `
    + `(1-2 предложения) объяснение по-русски, почему именно так.`;
}

function isValidTop10(top10, pool) {
  if (!Array.isArray(top10) || top10.length !== 10) return false;
  const poolCodes = new Set(pool.map((d) => d.code));
  const unique = new Set(top10);
  if (unique.size !== 10) return false;
  return top10.every((code) => poolCodes.has(code));
}

function fallbackTop10(pool) {
  return [...pool]
    .sort((a, b) => (a.standing ?? 999) - (b.standing ?? 999) || a.code.localeCompare(b.code))
    .slice(0, 10)
    .map((d) => d.code);
}

async function gridBotUserId() {
  const { rows } = await q(`select id from users where display_name = 'GridBot'`);
  if (rows.length === 0) throw new Error('GridBot не найден в users — применена ли миграция 0014?');
  return rows[0].id;
}

async function savePrediction(userId, raceId, codeToDriverId, top10Codes) {
  const positions = top10Codes.map((code) => codeToDriverId.get(code));
  await q(
    `insert into predictions (user_id, race_id, positions) values ($1, $2, $3::jsonb)
     on conflict (user_id, race_id) do update set positions = excluded.positions, updated_at = now()`,
    [userId, raceId, JSON.stringify(positions)],
  );
}

async function main() {
  const race = await openRaceThisWeek();
  if (!race) {
    console.log('aiplayer: нет открытой гонки с дедлайном на этой неделе, пропускаем');
    await close();
    return;
  }

  const pool = await poolDrivers(race.id);
  const codeToDriverId = new Map(pool.map((d) => [d.code, d.id]));
  const driverCodeOf = new Map(pool.map((d) => [d.id, d.code]));
  const ownResults = await seasonResults(race.season);
  const standingsMap = await pastSeasonStandings(pool.map((d) => d.id));

  let top10Codes;
  let reasoning = null;
  try {
    const prompt = buildPrompt(race, pool, driverCodeOf, ownResults, standingsMap);
    const answer = await askGemini(prompt);
    if (isValidTop10(answer.top10, pool)) {
      top10Codes = answer.top10;
      reasoning = answer.reasoning;
    } else {
      console.warn('aiplayer: ответ Gemini не прошёл валидацию, используем fallback');
      top10Codes = fallbackTop10(pool);
    }
  } catch (e) {
    console.warn(`aiplayer: Gemini недоступна (${e.message}), используем fallback`);
    top10Codes = fallbackTop10(pool);
  }

  const userId = await gridBotUserId();
  await savePrediction(userId, race.id, codeToDriverId, top10Codes);
  console.log(`aiplayer: прогноз на "${race.name}" сохранён — ${top10Codes.join('-')}`);

  if (reasoning) {
    const text = `🤖 GridBot поставил на <b>${escapeHtml(race.name)}</b>: ${top10Codes.join('-')}\n`
      + `Почему: ${escapeHtml(reasoning)}`;
    try {
      await sendTelegram(text);
      console.log('aiplayer: объяснение отправлено в Telegram');
    } catch (e) {
      console.warn(`aiplayer: не удалось отправить в Telegram: ${e.message}`);
    }
  }

  await close();
}

module.exports = { isValidTop10, fallbackTop10, escapeHtml, buildPrompt };

if (require.main === module) {
  main().catch((e) => {
    console.error('ERR', e.message);
    process.exit(1);
  });
}
```

- [ ] **Step 4: Прогнать тест ещё раз — убедиться, что проходит**

Run: `cd scripts/ai-player && node predict.test.js`
Expected: все строки `PASS`, последняя строка `ВСЕГО: 7 PASS, 0 FAIL`, код завершения 0.

- [ ] **Step 5: Синтаксическая проверка полного файла**

Run: `cd scripts/ai-player && node --check predict.js`
Expected: без вывода, код завершения 0.

- [ ] **Step 6: Добавить npm-скрипт теста**

В `scripts/ai-player/package.json` `"scripts"` уже содержит `"test": "node predict.test.js"` (добавлено
в Task 2 Step 1) — убедиться, что строка на месте, ничего менять не нужно.

- [ ] **Step 7: Commit**

```bash
git add scripts/ai-player/predict.js scripts/ai-player/predict.test.js
git commit -m "feat(ai-player): сбор данных, промпт Gemini, валидация и фолбэк, сохранение прогноза"
```

---

### Task 4: Расписание — режим `aiplayer` в существующем workflow

**Files:**
- Modify: `.github/workflows/telegram-notify.yml`

- [ ] **Step 1: Добавить `aiplayer` в опции ручного запуска**

Заменить (текущая строка 23):

```yaml
        options: [raceweek, deadline, results, remind, autoresults]
```

на:

```yaml
        options: [raceweek, deadline, results, remind, autoresults, aiplayer]
```

- [ ] **Step 2: Разделить четверговый триггер — утро теперь `aiplayer + deadline`, вечер как был**

Заменить (текущие строки 14-15):

```yaml
    - cron: '5 7 * * 4'    # Чт 10:05 МСК — deadline
    - cron: '5 16 * * 4'   # Чт 19:05 МСК — deadline
```

на:

```yaml
    - cron: '5 7 * * 4'    # Чт 10:05 МСК — aiplayer (прогноз ИИ до дедлайна) + deadline
    - cron: '5 16 * * 4'   # Чт 19:05 МСК — deadline
```

- [ ] **Step 3: Развести case-ветки для утреннего и вечернего четверга**

Заменить (текущая строка 41):

```bash
              '5 7 * * 3'|'5 16 * * 3'|'5 7 * * 4'|'5 16 * * 4') echo "modes=deadline" >> "$GITHUB_OUTPUT" ;;
```

на:

```bash
              '5 7 * * 3'|'5 16 * * 3'|'5 16 * * 4') echo "modes=deadline" >> "$GITHUB_OUTPUT" ;;
              '5 7 * * 4') echo "modes=aiplayer deadline" >> "$GITHUB_OUTPUT" ;;
```

- [ ] **Step 4: Добавить `GEMINI_API_KEY` в шаг записи `.env`**

Заменить (текущие строки 46-56):

```yaml
      - name: Записать .env для scripts/telegram и scripts/autoresults
        env:
          SUPABASE_DB_URL: ${{ secrets.SUPABASE_DB_URL }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          cat > .env <<EOF
          SUPABASE_DB_URL=$SUPABASE_DB_URL
          TELEGRAM_BOT_TOKEN=$TELEGRAM_BOT_TOKEN
          TELEGRAM_CHAT_ID=$TELEGRAM_CHAT_ID
          EOF
```

на:

```yaml
      - name: Записать .env для scripts/telegram, scripts/autoresults, scripts/ai-player
        env:
          SUPABASE_DB_URL: ${{ secrets.SUPABASE_DB_URL }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: |
          cat > .env <<EOF
          SUPABASE_DB_URL=$SUPABASE_DB_URL
          TELEGRAM_BOT_TOKEN=$TELEGRAM_BOT_TOKEN
          TELEGRAM_CHAT_ID=$TELEGRAM_CHAT_ID
          GEMINI_API_KEY=$GEMINI_API_KEY
          EOF
```

- [ ] **Step 5: Добавить `npm install` для `scripts/ai-player`**

Заменить (текущие строки 57-60):

```yaml
      - name: npm install
        run: |
          cd scripts/telegram && npm install
          cd ../autoresults && npm install
```

на:

```yaml
      - name: npm install
        run: |
          cd scripts/telegram && npm install
          cd ../autoresults && npm install
          cd ../ai-player && npm install
```

- [ ] **Step 6: Обработать режим `aiplayer` в шаге запуска**

Заменить (текущие строки 61-69):

```yaml
      - name: Запустить режимы
        run: |
          for m in ${{ steps.mode.outputs.modes }}; do
            if [ "$m" = "autoresults" ]; then
              node scripts/autoresults/fetch.js
            else
              node scripts/telegram/notify.js "$m"
            fi
          done
```

на:

```yaml
      - name: Запустить режимы
        run: |
          for m in ${{ steps.mode.outputs.modes }}; do
            if [ "$m" = "autoresults" ]; then
              node scripts/autoresults/fetch.js
            elif [ "$m" = "aiplayer" ]; then
              node scripts/ai-player/predict.js
            else
              node scripts/telegram/notify.js "$m"
            fi
          done
```

- [ ] **Step 7: Проверить YAML на валидность**

Run: `node -e "require('js-yaml')" 2>/dev/null || echo "js-yaml не установлен — проверить визуально"`
Если `js-yaml` недоступен (в проекте его нет как зависимости) — просто перечитать файл глазами и
убедиться, что отступы не съехали (это единственная проверка для этого шага, автоматической
валидации YAML в проекте нет).

- [ ] **Step 8: Commit**

```bash
git add .github/workflows/telegram-notify.yml
git commit -m "feat(ai-player): режим aiplayer в существующем telegram-notify workflow"
```

---

### Task 5: Документация — `.env.example` и README

**Files:**
- Modify: `.env.example`
- Create: `scripts/ai-player/README.md`

- [ ] **Step 1: Добавить переменную в `.env.example`**

Добавить в конец `.env.example`:

```

# --- ИИ-игрок лиги GridBot (scripts/ai-player, запускается по четвергам через GitHub Actions) ---
GEMINI_API_KEY=<ключ с aistudio.google.com — API keys>
```

- [ ] **Step 2: Написать `scripts/ai-player/README.md`**

```markdown
# ИИ-игрок лиги — GridBot

Раз в неделю (по четвергам, до дедлайна) ставит прогноз топ-10 наравне с людьми: смотрит пул гонки,
свои прошлые результаты сезона и итоги последних 3 сезонов F1 (через Jolpica), просит Gemini
предположить порядок. Если Gemini недоступна или ответ невалиден — подставляется прогноз по текущему
месту в чемпионате (без падения всего прогона). По четвергам после сохранения прогноза публикует в
общий Telegram-чат короткое объяснение выбора.

Играет как обычный участник лиги (аккаунт `GridBot`, миграция `0014_gridbot_user.sql`) — те же очки,
та же видимость в списке проголосовавших, тот же общий зачёт. Никакого отдельного UI.

## Разовая настройка

1. Открыть [Google AI Studio](https://aistudio.google.com/) → **Get API key** → создать новый ключ
   (бесплатный тариф — лимитов с запасом хватает при частоте «раз в неделю»).
2. В корневом `.env` (НЕ `.env.example`) добавить:
   ```
   GEMINI_API_KEY=<ключ из шага 1>
   ```
3. В GitHub-репозитории: **Settings → Secrets and variables → Actions → New repository secret** —
   добавить `GEMINI_API_KEY` с тем же значением (нужен для автозапуска по расписанию).

## Ручной запуск / проверка

```bash
cd scripts/ai-player
npm install   # один раз
npm run predict
```

Сработает только если есть открытая гонка с дедлайном на этой неделе — иначе выведет и завершится
без ошибки: `aiplayer: нет открытой гонки с дедлайном на этой неделе, пропускаем`.

При успехе выводит: `aiplayer: прогноз на "<Гонка>" сохранён — VER-NOR-...` и (если это реально
четверг и Gemini ответила) `aiplayer: объяснение отправлено в Telegram`.

Проверить результат: открыть вкладку «Прогноз» под своим аккаунтом на этой гонке — в списке
«Проголосовали» должен появиться GridBot (сам прогноз, как и у всех, скрыт до дедлайна).
```

- [ ] **Step 3: Commit**

```bash
git add .env.example scripts/ai-player/README.md
git commit -m "docs(ai-player): инструкция по настройке Gemini API key"
```

---

### Task 6 (ручной шаг пользователя): API-ключ Gemini + смоук-тест на реальной гонке

Эта задача не выполняется сабагентом — получение ключа в Google AI Studio делает пользователь лично,
по инструкции из `scripts/ai-player/README.md`.

- [x] Пройти шаги 1-3 из README (получить ключ на aistudio.google.com, заполнить `.env`, добавить
      GitHub-секрет `GEMINI_API_KEY`). Выполнено 2026-07-24.
- [x] Запустить `cd scripts/ai-player && npm install && npm run predict` вручную, на реальной открытой
      гонке (Hungarian GP, race id 12). **Найдено и починено по ходу:** дефолтная модель
      `gemini-2.0-flash` вернула HTTP 429 (`limit: 0` — нулевая бесплатная квота на этом ключе, не
      транзиентный rate-limit); `gemini-2.5-flash` отработала штатно. Дефолт в `lib.js` изменён
      (коммит `bc35492`), после чего `npm run predict` без `GEMINI_MODEL` сработал из коробки.
- [x] Проверить консольный вывод. Реальный прогон: `aiplayer: прогноз на "Hungarian Grand Prix"
      сохранён — ANT-RUS-LEC-VER-HAM-NOR-PIA-HAD-LIN-SAI`, плюс ожидаемо `не удалось отправить в
      Telegram: TELEGRAM_BOT_TOKEN не найден в .env` (локально токена нет намеренно — пользователь
      попросил не спамить в живой чат во время теста; это и есть штатная защита, не баг).
- [x] Открыть вкладку «Прогноз»/проверить эквивалент — вместо UI сверено напрямую через ту же RPC,
      что использует фронтенд: `predicted_user_ids(12)` возвращает GridBot вместе с реальными
      игроками (Dim, Pavel) — список «Проголосовали» покажет его как обычного участника.
- [ ] ~~Если сегодня четверг — проверить Telegram~~ — не применимо к этому тесту (намеренно без
      токена локально, см. выше). Реальная проверка Telegram-сообщения — по факту первого боевого
      запуска в четверг по расписанию.
- [ ] ~~`workflow_dispatch` в GitHub Actions~~ — сознательно пропущено по решению пользователя (риск
      реального сообщения в общий чат, т.к. в CI `TELEGRAM_BOT_TOKEN` настоящий). Первый реальный
      прогон в CI — по расписанию, в ближайший четверг.
- [x] Результат подтверждён пользователем («ну нормально можно транслировать на сайт») — обновляем
      `MEMORY.md` и коммитим.

---

### Task 7: Финальное ревью ветки + merge

Стандартный процесс проекта (`superpowers:finishing-a-development-branch`): прогнать все тесты
(`scripts/db/gridbot.test.js`, `scripts/ai-player/predict.test.js`, плюс существующие regression-тесты
`scripts/db/rls.test.js`/`security_grants.test.js`/`predicted_user_ids.test.js`, `npx tsc --noEmit` —
фронтенд этой веткой не тронут, но чек не помешает), дождаться подтверждения Task 6 от пользователя,
финальный ревью-проход по всей ветке, merge в `main`, push. Деплоя фронтенда не требует (бэкенд-скрипт
+ GitHub Actions, как autoresults/telegram-notify).

---

## Self-Review

**Spec coverage:** §2 (GridBot как обычный участник + риск auth.users) → Task 1, риск уже снят
эмпирической проверкой перед написанием плана. §3 (источники данных: пул/свой сезон/прошлые сезоны
Jolpica/явно без F2) → Task 3 (`poolDrivers`, `seasonResults`, `pastSeasonStandings`, промпт с явной
пометкой «новичок»). §4 (Gemini, JSON-режим, модель через env) → Task 2 (`askGemini` в `lib.js`).
§5 (валидация + фолбэк) → Task 3 (`isValidTop10`, `fallbackTop10`), протестировано. §6 (сохранение
прогноза, тот же способ поиска открытой гонки, что у `notify.js`) → Task 3 (`openRaceThisWeek`,
`savePrediction`). §7 (Telegram-объяснение только по четвергам, без отдельной проверки дня недели —
достаточно расписания) → Task 3 (`main()`, отправка только если `reasoning` не `null`) + Task 4
(cron только на утренний четверг). §8 (запуск через существующий workflow, режим `aiplayer`, до
дедлайна) → Task 4. §9 (секреты, миграция) → Task 1 + Task 5 + Task 6. §10 (тестирование — чистая
логика unit-тестом, БД-часть в откатываемой транзакции, финал — живой смоук) → Task 1 Step 3, Task 3
Step 1-4, Task 6. §11 (явно вне скоупа — не добавлено ничего лишнего: нет UI, нет коэффициентов,
нет отдельного F2-источника).

**Placeholder scan:** нет TBD/TODO, весь код полный и рабочий, команды с ожидаемым выводом.
Единственное намеренное исключение — Task 4 Step 7 («проверить глазами», т.к. в проекте нет
YAML-линтера как зависимости) — это не placeholder, а честное описание единственного доступного
способа проверки.

**Type consistency:** `q`/`close`/`fetchJolpica`/`askGemini`/`escapeHtml`/`sendTelegram` — сигнатуры
одинаковые в Task 2 (объявление в `lib.js`) и Task 3 (импорт и использование в `predict.js`).
`isValidTop10(top10, pool)`/`fallbackTop10(pool)` — одинаковые сигнатуры в определении (Task 3 Step 3)
и в тесте (Task 3 Step 1). UUID GridBot (`8093e42f-cc5e-4c18-8aa8-2dfa50e972c1`) — один и тот же в
Task 1 (миграция + тест) и подразумеваемо в Task 3 (`gridBotUserId()` ищет по `display_name='GridBot'`,
не по хардкоду UUID — сознательно, чтобы не дублировать константу между файлами).
